apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xiangjiandao.fullname" . }}-test-connection"
  labels:
    {{- include "xiangjiandao.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xiangjiandao.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
